


﻿app.filter("passwordFilter", function () {

    return function (word) {
        if (word !== undefined) {
            var wordToSend = "";
            for (var i = 0; i < word.length; i++) {
                wordToSend += "*";
            }
            return wordToSend;
        }
        else
        	return wordToSend = "********";
    }

});
